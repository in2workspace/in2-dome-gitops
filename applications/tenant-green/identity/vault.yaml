apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-green-vault
  namespace: argocd
  labels:
    purpose: tenant-green-identity
spec:
  destination:
    namespace: tenant-green-identity
    server: https://kubernetes.default.svc
  project: default
  source:
    path: charts/tenant-green/identity/vault
    repoURL: https://github.com/in2workspace/in2-dome-gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true