apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tenant-blue-vault
  namespace: argocd
  labels:
    purpose: tenant-blue-identity
spec:
  destination:
    namespace: tenant-blue-identity
    server: https://kubernetes.default.svc
  project: default
  source:
    path: charts/tenant-blue/identity/vault
    repoURL: https://github.com/in2workspace/in2-dome-gitops
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true